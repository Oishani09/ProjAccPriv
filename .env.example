# MySQL Configuration (Legacy)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=edi_data

# MongoDB Configuration (Commented Out — replaced by BigQuery)
# MONGO_URI=mongodb://localhost:27017
# MONGO_DB_NAME=health_enroll

# GCP Configuration
GCP_PROJECT_ID=your-gcp-project-id
GOOGLE_APPLICATION_CREDENTIALS=path/to/your/service-account.json
GCP_DOCS_BUCKET=your-enrollment-docs-bucket
GCP_DATASET_ID=health_enroll
GCP_TABLE_ID=members_test